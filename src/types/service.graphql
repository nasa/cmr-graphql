type Service {
  concept_id: String!
  long_name: String
  name: String
  native_id: String
  provider_id: String
  revision_id: Int
}

type Query {
  services(
    concept_id: [String]!,
    page_size: Int!
  ): [Service]
  service(
    concept_id: String!
  ): Service!
}
