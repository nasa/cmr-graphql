type Variable {
  concept_id: String!
  long_name: String
  name: String
  native_id: String
  provider_id: String
  revision_id: Int
}

type Query {
  variables(
    concept_id: [String]!,
    page_size: Int!
  ): [Variable]
  variable(
    concept_id: String!
  ): Variable!
}
