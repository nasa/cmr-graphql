type Acl {
  revisionId: Int
  conceptId: String
  identityType: String  
  location: String
}
input AclInput {
  "Options to provide to CMR"
  options: JSON

}
type Query {
  acl (
    params: AclInput
  ): Acl
}